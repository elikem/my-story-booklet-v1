<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1">

  <title>My Story Booklet</title>

  <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>

  <!-- HTML5 shim and Respond.js for IE8 support of HTML5 elements and media queries -->
  <!-- WARNING: Respond.js doesn't work if you view the page via file:// -->
  <!--[if lt IE 9]>
  <script src="https://oss.maxcdn.com/html5shiv/3.7.2/html5shiv.min.js"></script>
  <script src="https://oss.maxcdn.com/respond/1.4.2/respond.min.js"></script>
  <![endif]-->

</head>
<body class="sidebar-push">

<nav class="navbar navbar-default navbar-fixed-top">
  <div class="container full-width">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle visible-xs collapsed pull-left" data-toggle="collapse" data-target="#main-navigation">
        <i class="md md-menu"></i>
      </button>
      <a class="navbar-brand" href="/">My Story Booklet</a>
      <button type="button" class="navbar-toggle pull-right" id="showRightPush">
        <i class="md md-more-vert"></i>
      </button>
    </div>
    <div class="collapse navbar-collapse" id="main-navigation">
      <ul class="nav navbar-nav">
        <li class="<%= user_signed_in? ? current_class?(story_path(current_user.username)) : '' %>">
          <a href="<%= user_signed_in? ? story_path(current_user.username) : new_user_session_path %>">My Story</a></li>
        <li><%= link_to 'Sample Booklet', '/Cru_Soaring_Booklet.pdf', :target => '_blank' %></li>
        <li class="<%= current_class?(training_worksheet_path) %>">
          <a href="<%= training_worksheet_path %>">Training Worksheet</a></li>
      </ul>
      <ul class="nav navbar-nav navbar-right hidden-xs">
        <% if current_user.try(:admin?) %>
            <button class="btn btn-default-light navbar-btn pull-left" style="background-color: orange;"> 
              <a href="<%= admin_path %>" style="color: white;"><i class="md md-settings"></i> </a>
            </button>
        <% end %>
        <li class="dropdown"> 
          <a href="#" style="text-decoration: none; padding: 0 !important; color: #f4511e !important;" data-toggle="dropdown"><i class="md md-wrap-text"></i> Menu <span class="caret"></span></a> 
          <ul class="dropdown-menu dropdown-caret dropdown-caret-right dropdown-menu-auto"> 
            <li><%= link_to 'mailto:tsw@cru.org' do %><i class="md md-mail" style="color: #616161;"></i><span style="color: #616161;"> Contact Us</span><% end %></li>
            <li><%= link_to edit_user_registration_path do %><i class="md md-settings" style="color: #616161;"></i><span style="color: #616161;"> Account Settings</span><% end %></li>
            <li class="divider"></li>
            <li><%= link_to destroy_user_session_path do %><i class="md md-vpn-key text-orange"></i><span class="text-orange"> Logout</span><% end %></li>
          </ul>
        </li>
      </ul>
    </div>
  </div>
</nav>

<!-- Admin Navigation -->
<%= render 'admin/admin_navigation_panel' %>

<div class="sidebar right-side" id="sidebar-right">
  <!-- Wrapper Reqired by Nicescroll -->
  <div class="nicescroll">
    <div class="wrapper">
      <ul class="nav nav-sidebar" id="sidebar-menu">
        <% if current_user.try(:admin?) %>
            <li><a href="<%#= admin_path %>"><i class="md md-lock-open"></i> Admin</a></li>
        <% end %>
        <li><a href="mailto:tsw@cru.org"><i class="md md-email"></i> Contact Us</a></li>
        <li><a href="<%= edit_user_registration_path %>"><i class="md md-settings"></i> Settings</a></li>
        <li>
          <a href="<%= user_signed_in? ? destroy_user_session_path : new_user_session_path %>"><i class="md md-exit-to-app"></i> <%= user_signed_in? ? 'Logout' : 'Sign In' %>
          </a>
        </li>
      </ul>
    </div>
  </div>
</div>


<div class="container">
  <% flash.each do |type, message| %>
      <div id="notification" data-position="top-right" data-type="<%= type %>" class="display-none">
        <%= message %>
      </div>
  <% end %>

  <%= yield %>

  <div class="footer">
    <p>&copy; 2015 - <strong>MY STORY BOOKLET</strong>. BY THE SIGNIFICANCE PROJECT.</p>
    <ul>
      <li><a href="mailto:tsw@cru.org">Contact Us</a></li>
    </ul>
  </div>
</div>


<div class="overlay-disabled"></div>


<!-- Plugins -->
<%= javascript_include_tag 'blueprint/plugins.min' %>

<!-- Loaded only in index.html for demographic vector map-->
<%#= javascript_include_tag 'blueprint/jvectormap' %>

<!-- App Scripts -->
<%= javascript_include_tag 'blueprint/scripts' %>

<script>
    $("a[data-toggle=dropdown]").dropdown();
</script>

</body>
</html>